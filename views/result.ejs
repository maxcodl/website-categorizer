<!DOCTYPE html>
<html>
  <head>
    <title>Website Categorization Result</title>
  </head>
  <body>
    <h1>Website Categorization Result</h1>
    <% if (categories && categories.length > 0) { %>
    <p>
      Categories assigned based on keyword match: <%= categories.join(', ') %>
    </p>
    <% } else { %>
    <p>
      No categories were assigned based on keywords. Using OpenAI to categorize.
    </p>
    <% } %> <% if (categories && categories.length > 0) { %>
    <p>
      Categories assigned based on keyword match: <%= categories.join(', ') %>
    </p>
    <% } %>
  </body>
</html>
